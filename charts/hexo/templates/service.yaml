apiVersion: v1
kind: Service
metadata:
  name: {{ include "hexo.fullname" . }}
  labels:
    {{- include "hexo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hexo.selectorLabels" . | nindent 4 }}
