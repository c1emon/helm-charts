apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "_libapp.fullname" . }}-test-connection"
  labels:
    {{- include "_libapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "_libapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
