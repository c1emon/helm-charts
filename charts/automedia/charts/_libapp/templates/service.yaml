apiVersion: v1
kind: Service
metadata:
  name: {{ include "_libapp.fullname" . }}
  labels:
    {{- include "_libapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "_libapp.selectorLabels" . | nindent 4 }}
