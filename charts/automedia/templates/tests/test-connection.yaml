apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "automedia.fullname" . }}-test-connection"
  labels:
    {{- include "automedia.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "automedia.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
